<h1>Available Equipment Listing</h1>
<table class="table table-striped">
    <thead>
        <tr>
            <th>Id</th>
            <th>Type</th>
            <th>Description</th>
            <th>Availability</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.Type</td>
                <td>@item.Description</td>
                <td>@(item.IsAvailable ? "Available" : "Not Available")</td>
            </tr>
        }
    </tbody>
</table>
